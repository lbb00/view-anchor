# Changesets

This folder stores release notes and version bumps for `view-anchor`.

Run `pnpm changeset` for every change that should be included in the next npm release. The release workflow creates a version PR from pending Changesets and publishes after that PR is merged into `main`.
